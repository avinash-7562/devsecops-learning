In IT, most of the operations are CRUD

CRUD - Create, Read, Update, Delete

Absolute path - full path
Relative path - partial path

/root- root user home directory
/home/<username> - for other users home directory

Everything is command and text in linux

command syntax: <command-name> <input> <options>
inputs and options are optional based on command usage

system users - id <= 1000 in /etc/passwd and has nologin